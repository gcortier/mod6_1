services:
  backend:
    build: ./backend
    container_name: backend
    ports:
      - "8000:${API_PORT}"
    # env_file:
    #   - .env
    # volumes:
    #   - ./logs_mnistretrain:/logs  # Montage du volume pour stocker les logs
    environment:
      - API_PORT=${API_PORT}
    networks:
      - app_network
    restart: unless-stopped

  frontend:
    build: ./frontend
    container_name: frontend
    ports:
      - "8501:${STREAMLIT_PORT}"
    env_file:
      - .env
    # volumes:
    #   - ./logs_mnistretrain:/logs  # Montage du volume pour stocker les logs
    environment:
      - API_URL=${API_URL:-http://backend:8000}
      - MLFLOW_TRACKING_URI=${MLFLOW_TRACKING_URI:-http://mlflow:5000}
    depends_on:
      - backend
    networks:
      - app_network 

  prometheus:
    image: prom/prometheus:latest
    ports:
      - "${PROMETHEUS_PORT}:${PROMETHEUS_PORT}"
    environment:
      - API_PORT=${API_PORT}
    volumes:
      - ./prometheus_mnistretrain/prometheus.yml:/etc/prometheus_mnistretrain/prometheus.yml
    command:
      - "--config.file=/etc/prometheus_mnistretrain/prometheus.yml"
    depends_on:
      - backend
    networks:
      - app_network

  grafana:
    image: grafana/grafana:latest
    ports:
      - "${GRAFANA_PORT}:${GRAFANA_PORT}"
    environment:
      - GF_SECURITY_ADMIN_USER=${GRAFANA_ADMIN_USER}
      - GF_SECURITY_ADMIN_PASSWORD=${GRAFANA_ADMIN_PASSWORD}
      - API_PORT=${API_PORT}
      - PROMETHEUS_PORT=${PROMETHEUS_PORT}
    volumes:
      - ./grafana_mnistretrain/provisioning/dashboards:/etc/grafana/provisioning/dashboards
      - ./grafana_mnistretrain/provisioning/datasources:/etc/grafana/provisioning/datasources
    depends_on:
      - prometheus
    networks:
      - app_network

  uptime-kuma:
    image: louislam/uptime-kuma:latest
    container_name: uptime_kuma
    ports:
      - "3001:3001"
    volumes:
      - uptime-kuma-data:/app/data
    restart: unless-stopped
    networks:
      - app_network

  node-exporter:
    image: prom/node-exporter:latest
    container_name: node_exporter
    ports:
      - "9100:9100"
    networks:
      - app_network

  mlflow:
    build: ./mlflow
    container_name: mlflow
    ports:
      - "5000:5000"
    volumes:
      - ./mlflow/mlruns:/mlflow/mlruns
      - ./mlflow/artifacts:/mlflow/artifacts
    networks:
      - app_network
    restart: unless-stopped


volumes:
  uptime-kuma-data:


networks:
  app_network:
    driver: bridge
